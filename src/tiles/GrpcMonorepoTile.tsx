// Placeholder — fleshed out in a later commit.
export default function GrpcMonorepoTile() {
  return (
    <div className="flex h-full flex-col">
      <header>
        <h3 className="font-mono text-sm font-medium text-foreground">grpc-monorepo-starter</h3>
        <p className="mt-1 text-xs text-muted">proto → multi-language clients</p>
      </header>
      <div className="mt-4 flex-1 rounded-lg border border-dashed border-border/60 p-3 text-xs text-muted">
        wiring up
      </div>
    </div>
  );
}
