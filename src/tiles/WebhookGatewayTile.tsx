// Placeholder — fleshed out in a later commit.
export default function WebhookGatewayTile() {
  return (
    <div className="flex h-full flex-col">
      <header>
        <h3 className="font-mono text-sm font-medium text-foreground">webhook-gateway</h3>
        <p className="mt-1 text-xs text-muted">retry-backoff visualiser</p>
      </header>
      <div className="mt-4 flex-1 rounded-lg border border-dashed border-border/60 p-3 text-xs text-muted">
        wiring up
      </div>
    </div>
  );
}
